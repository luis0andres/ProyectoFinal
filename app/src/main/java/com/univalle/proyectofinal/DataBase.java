package com.univalle.proyectofinal;

import android.content.ContentValues;
import android.content.Context;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteOpenHelper;

//public class DataBase extends SQLiteOpenHelper {
   /* public static final int VERSION= 1;
    public static final String DBname="Deposito.db";
    public DataBase(Context context) {
        super(context, DBname, null, VERSION);
    }

    @Override
    public void onCreate(SQLiteDatabase db) {
        //crear la tabla
        db.execSQL("CREATE TABLE " + Tabla.LawyerEntry.TableName + " ( "
        + Tabla.LawyerEntry._ID + " INTEGER PRIMARY KEY AUTOINCREMENT ,"
        + Tabla.LawyerEntry.ID + "TEXT NOT NULL,"
        + Tabla.LawyerEntry.NOMBRE + "TEXT NOT NULL,"
        + Tabla.LawyerEntry.DESCRIPCION + "TEXT NOT NULL,"
        + Tabla.LawyerEntry.CANTIDAD + "TEXT NOT NULL,"
        + Tabla.LawyerEntry.AVATAR + "TEXT,"
        + "UNIQUE (" + Tabla.LawyerEntry.ID + "))");

        //valores
        ContentValues values =new ContentValues();
    }

    @Override
    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {

    }*/
//}
